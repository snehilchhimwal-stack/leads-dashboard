<!-- DASHBOARD TEMPLATE (DASH-XXX). Generic skeleton + the sections below. -->

# <DASH-ID> — <Name>

<Table header block, Purpose, Responsibilities — as `component-record-template.md`.>

## Who / what uses it
<Which roles/people, and in what situation.>

## Entry points
<URL(s), how a user gets here, the sign-in gate.>

## Tabs it contains
<`TAB-XXX` list — one line each. Detail lives on the TAB records.>

## Top-level buttons / actions
<Only dashboard-wide actions here (source switch, refresh). Tab-specific
buttons live on their `TAB-XXX` record as `BTN-XXX` sub-tables.>

## HTML / CSS structure
<`HTML-XXX` / `CSS-XXX` sub-tables for the structural regions and the
style system, if they clear the threshold. Otherwise prose.>

## Data sources
<`SHEET-XXX` / `EXT-XXX` IDs this dashboard reads.>

## Data written / modified
<`SHEET-XXX` IDs this dashboard writes back to, and via which `JS-XXX`.>

## What should / should not be changed
<Load-order constraints, the two-independent-halves rule, etc.>

## Known limitations
<From `LOGIC_AUDIT.md` / `HANDOVER.md` §8.>

<Then: Architecture relationship, Related documentation, Relationships,
Source of truth, Validation, Version/change reference, Revalidation
trigger, Handover relationship, Lifecycle/retention, Next action,
Closure evidence — as generic.>
