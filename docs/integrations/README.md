# integrations

EXT- records for the 4 external integrations: Google Sheets API v4
(`EXT-001`), Gmail send (`EXT-002`), Google Identity/OAuth (`EXT-003`),
jsPDF + jspdf-autotable (`EXT-004`). Written by `DOC-033`, Phase 3.
See `../INDEX.md`.
