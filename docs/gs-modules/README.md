# gs-modules

GS- records for the 13 production `*.gs` files (`Tests_*.gs` and
`RmHierarchy.private.gs` excluded per `DOC-007`), each with its Trigger
Schedule + `setupXxx()` re-run condition + `FN-XXX` sub-table. Written by
`DOC-029`, Phase 3 (`FN-174`..`FN-254`). See `../INDEX.md`.
