# Change record — the Documentation Project build + hardening (2026-09-10)

**Type:** documentation-only. No production `js/*.js` / `.gs` / `dashboard.html`
change (`git log c82ec67..HEAD -- js/*.js *.gs dashboard.html` is empty as
of this record).
**Why this record exists:** the Governance Model's Definition of Done
point 14 asks for a `docs/changes/` record for any closure. Every closure
below was prompted by the doc project itself, not a code change — so this
is one combined record for the whole build rather than one per component.
The **next real post-catalog code change** gets its own
`docs/changes/<date>-<sha>.md` (`check-catalog.py` check E prints the
target filename).

---

## What changed

| Date | Commit(s) | Task | What |
|---|---|---|---|
| 2026-09-10 | `c82ec67` → `99170a8` | `DOC-001`–`DOC-050`, Governance Model, `CONSOLIDATED` | Built `docs/`: 69 `Closed + Monitored` records (1 `DASH`, 8 `TAB` + `BTN-001..022` + `UI-001..014`, 24 `JS` + `FN-001..254`, 13 `GS`, 14 `SHEET`, 4 `EXT`, 5 `DATA`), `INDEX.md`, `_templates/`, the `HOW_TO_*` guides, `PRE_SHIP_DOCUMENTATION_CHECKLIST.md`, `RELATIONSHIP_MAP.md`, `NAMING_CONVENTIONS.md`, all `_planning/*` inventories + verification passes. |
| 2026-09-10 | `cc90ee3` | `t-tf-7e4d0dffdf6c` | `CLAUDE.md` / `HANDOVER.md` doc-accuracy (C-1..C-4, C-7): script-load-order, the pre-split `<script>` chain, the §1 `.gs` list, §7.2 test-harness pointer, §5 "every 6h" → "4×/day". |
| 2026-09-10 | `cddbd17`, `de693e5` | `t-tf-47c37923c3bd` | `Depends On` / `Used By` reciprocity: additive pass (+59/+171 back-links) then a **prune** so all 69 records match their `INDEX.md` row exactly — 0 one-directional pairs (`recip_verify.py`). |
| 2026-09-10 | `d2b5fda` | `t-tf-cc97c00a3839` | Forensic completeness audit (`FORENSIC_COMPLETENESS_AUDIT_2026-09-10.md`). |
| 2026-09-10 | `3d2db17`, `cb5afb1`, `adb40d4`, `acc7cbe` | `t-tf-5ad22d8e4c2e` | Change-control loop **detection half**: `test/check-catalog.py` (checks A–F), `fetch-depth: 0`; fixed the 3 live doc contradictions (plan governance section, `INDEX.md` footer, `HANDOVER.md` §9.7 headline). |

## Which records went `Stale`

**None.** No code changed, so nothing was invalidated. The reciprocity
pass rewrote `## Relationships` on 54 record files but did not change any
component's meaning, `Record Status`, or `Last Verified` — every record
stayed `Closed + Monitored` at `c82ec67`.

## Revalidation task

N/A — no revalidation was triggered (no code drift). The follow-up work
that remains is tracked in `_planning/OPEN_ITEMS.md` (retention `TBD`s,
the deep §9 `HANDOVER.md` sweep, P2/P3 of the audit).

## Closure evidence

- `test/run-gs-tests.js` — green every push (GitHub Actions "Apps Script
  tests", runs #66–#78+).
- `test/check-catalog.py` — checks A/B/C/F green locally and on the
  runner (run #76 onward).
- `recip_verify.py` — 69/69 records == `INDEX.md`, 0 one-directional.
- `_planning/completeness-verification.md` (`DOC-039`),
  `reference-verification.md` (`DOC-040`), `consistency-check.md`
  (`DOC-041`) — the Phase 5 passes.
- `docs/validation/README.md` — per-area evidence index.
