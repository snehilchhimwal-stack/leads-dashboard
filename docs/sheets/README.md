# sheets

SHEET- base records for the 14 Google Sheet tabs. Written by `DOC-032`,
Phase 3 (non-lifecycle scope). The `## Data Lifecycle` section is
confirmed for `Movement_Log` / `Daily_RM_Issues` (7d) and
`Comment_History` / `Unmatched_Comments_Log` (retention model); the other
10 carry `TBD` for `DOC-036` (Phase 4). See `../INDEX.md`.
